# CNN_Classification
A deep learning project that uses a Convolutional Neural Network (CNN) to classify images into different categories. This project demonstrates how CNN architectures automatically extract features from images and perform accurate classification.
